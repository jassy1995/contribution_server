export const ContributionStatus = {
  PENDING: 'pending',
  COMPLETED: 'completed',
  PARTIAL: 'partial',
};
export enum ContributionCategories {
  SET_ONE = 'set-one',
  SET_TWO = 'set-two',
  SET_THREE = 'set-three',
}
export const UserRoles = {
  USER: 'user',
  ADMIN: 'admin',
} as const;
